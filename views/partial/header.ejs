<header>
    <h1>Movie App</h1>
    <nav>
        <a href="/home">Home</a>

        <% if (isAdmin) { %>
            <a href="/admin">Admin</a>
        <% } %>

        <a href="/account">Account</a>

        <% if (isLoggedIn) { %>
            <a href="/logout">Logout</a>
        <% } else { %>
            <a href="/login">Login</a>
        <% } %>
    </nav>
    <hr>
</header>