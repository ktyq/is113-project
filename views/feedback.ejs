<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="stylesheet" href="/default.css">
</head>

<body>
    <%- include('partial/header', { user: user }) %>

    <header>
        <h1>Feedback</h1>
    </header>

    <main>
        <section>
            <h2>Submit Feedback</h2>
            <hr>
            <form action="/feedback" method="POST">
                <label>Type:</label><br>
                <select name="type" required>
                    <option value="" disabled selected>Select type</option>
                    <option value="feedback">Feedback</option>
                    <option value="bug">Bug Report</option>
                    <option value="report">User Report</option>
                </select>
                <br>

                <label>Related URL (optional):</label><br>
                <input type="text" name="url" placeholder="e.g. /movie/view?id=...">
                <br>

                <label>Notes:</label><br>
                <textarea name="notes" maxlength="256" placeholder="Describe your feedback..." required style="width: 100%; min-height: 80px;"></textarea>
                <br>

                <button type="submit">Submit Feedback</button>
            </form>
        </section>

        <br>

        <section>
            <h2>My Feedback History</h2>
            <% if (feedbacks && feedbacks.length > 0) { %>
                <table border="1">
                    <thead>
                        <tr>
                            <th>Type</th>
                            <th>Notes</th>
                            <th>URL</th>
                            <th>Status</th>
                            <th>Date</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% feedbacks.forEach(function(fb) { %>
                            <tr>
                                <td><%= fb.type %></td>
                                <td><%= fb.notes %></td>
                                <td>
                                    <% if (fb.url) { %>
                                        <a href="<%= fb.url %>">Link</a>
                                    <% } else { %>
                                        -
                                    <% } %>
                                </td>
                                <td><%= fb.status %></td>
                                <td><%= fb.createdAt.toLocaleDateString() %></td>
                                <td>
                                    <form action="/feedback/delete/<%= fb._id %>" method="POST" onsubmit="return confirm('Delete this feedback?')">
                                        <button type="submit">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <p>You haven't submitted any feedback yet.</p>
            <% } %>
        </section>
    </main>

    <br>
    <a href="/index">Back to Dashboard</a>
</body>

</html>